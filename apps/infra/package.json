{
  "name": "infra",
  "version": "1.0.0",
  "scripts": {
    "prettier": "prettier --write .",
    "diff:dev": "cdk diff -c stage=Dev",
    "diff:prod": "cdk diff -c stage=Prod",
    "deploy:dev": "cdk deploy -c stage=Dev --require-approval never --telemetry-file=cdk.out/telemetry.json",
    "deploy:prod": "cdk deploy -c stage=Prod --require-approval never --telemetry-file=cdk.out/telemetry.json",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "Franco Mariño <franco.marino.aquise@gmail.com>",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@packages/lambda": "workspace:*",
    "aws-cdk-lib": "2.238.0",
    "constructs": "10.4.5"
  },
  "devDependencies": {
    "ts-node": "10.9.2",
    "esbuild": "0.27.3"
  }
}
